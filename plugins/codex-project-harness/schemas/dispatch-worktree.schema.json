{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["id", "run_id", "task_id", "agent_id", "branch_name", "worktree_path", "status", "created_at", "cleaned_at"],
  "properties": {
    "id": { "type": "string" },
    "run_id": { "type": "string" },
    "task_id": { "type": "string" },
    "agent_id": { "type": "string" },
    "branch_name": { "type": "string" },
    "worktree_path": { "type": "string" },
    "status": { "type": "string", "enum": ["active", "cleaned", "failed"] },
    "created_at": { "type": "string" },
    "cleaned_at": { "type": "string" }
  }
}
