{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Codex Project Harness External Session Verification",
  "type": "object",
  "required": ["id", "session_id", "verifier", "conclusion", "commit_sha", "origin", "token_status", "token_reason", "effective_trust", "receipt_provenance", "created_at"],
  "properties": {
    "id": { "type": "string" },
    "session_id": { "type": "string" },
    "verifier": { "type": "string" },
    "conclusion": { "type": "string", "enum": ["verified", "failed"] },
    "commit_sha": { "type": "string" },
    "origin": { "type": "string", "enum": ["manual", "connector"] },
    "verification_token": { "type": "string" },
    "token_status": { "type": "string" },
    "token_reason": { "type": "string" },
    "effective_trust": { "type": "string", "enum": ["local-only", "human-confirmed", "connector", "legacy-untrusted"] },
    "receipt_provenance": { "type": "string" },
    "external_link": { "type": "string" },
    "created_at": { "type": "string" }
  },
  "additionalProperties": true
}
