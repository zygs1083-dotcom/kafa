{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Codex Project Harness Quality Gate",
  "type": "object",
  "required": ["id", "sequence", "cycle_id", "candidate_sha", "gate_status", "superseded_by", "gate", "reviewed_commit", "reviewer_context", "result"],
  "properties": {
    "id": { "type": "string" },
    "sequence": { "type": "integer" },
    "cycle_id": { "type": "string" },
    "candidate_sha": { "type": "string" },
    "gate_status": { "type": "string", "enum": ["active", "superseded", "legacy-ambiguous"] },
    "superseded_by": { "type": "string" },
    "gate": { "type": "string" },
    "reviewed_commit": { "type": "string" },
    "evidence_commit": { "type": "string" },
    "diff_hash": { "type": "string" },
    "base_commit": { "type": "string" },
    "head_commit": { "type": "string" },
    "tracked_diff_hash": { "type": "string" },
    "project_revision": { "type": "integer" },
    "reviewer_context": {
      "type": "string",
      "enum": ["fresh", "same-context-degraded", "external"]
    },
    "result": {
      "type": "string",
      "enum": ["pass", "fail", "conditional", "blocked"]
    },
    "blocking_findings": { "type": "string" },
    "commands": { "type": "string" },
    "evidence": { "type": "string" },
    "residual_risk": { "type": "string" },
    "reviewer_session_id": { "type": "string" },
    "reviewer_attestation_id": { "type": "string" },
    "review_trust_level": { "type": "string", "enum": ["local-only", "human-confirmed", "connector", "legacy-untrusted"] }
  },
  "additionalProperties": true
}
