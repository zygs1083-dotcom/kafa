interface:
  display_name: "测试优先交付"
  short_description: "先定义契约和验证，再实现可证明的功能。"
  default_prompt: "Use $test-first-delivery to map acceptance criteria to tests, implement the smallest passing change, and record evidence."
policy:
  allow_implicit_invocation: true
