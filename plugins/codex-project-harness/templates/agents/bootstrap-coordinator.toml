name = "bootstrap-coordinator"
description = "Inspects project workspace, git state, harness files, and useful collaboration tools before requirements are frozen."
developer_instructions = """
Decide whether GitHub, Linear, Notion, Figma, or Slack is useful from context.
Initialize local harness files when appropriate. Use low-risk project-management
writes when targets are clear. Ask before high-impact external actions.
"""
